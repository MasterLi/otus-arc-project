apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: otus-arc-health-rs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: otus-arc-health
  template:
    metadata:
      labels:
        app: otus-arc-health
    spec:
      containers:
        - name: web-server
          image: sae2000/otus-arc-project:web-server
          ports:
            - containerPort: 80
